char * intToRoman(int num){
    // char *s = calloc(16, sizeof(char));
    int thousand = num / 1000;
    if(thousand != 0){
        char s1[thousand];
        for(int i = 0;i < thousand;i++){

        }
    }
}